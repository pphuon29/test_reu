<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Erreur' %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="mt-5">
    <div class="container">
        <div class="alert alert-danger">
            <h1 class="alert-heading"><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Une erreur est survenue' %></h1>
            <%# Affiche le message spécifique passé par la route %>
            <% if (typeof message !== 'undefined' && message) { %>
                <p><%= message %></p>
            <% } else { %>
                <p>Une erreur inattendue s'est produite. Veuillez réessayer plus tard.</p>
            <% } %>
        </div>
        <a href="/" class="btn btn-secondary mt-3">Retour à l'accueil</a>
        </div>
    </body>
</html>