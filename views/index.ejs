<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    </head>
<body>
    <h1>Bienvenue sur le Planificateur de Réunions</h1>

    <% if (user) { %>

        <%# Section pour utilisateur connecté %>

        <div class="alert alert-success" role="alert">
            Bonjour, <%= user.name %> ! (Connecté en tant que <%= user.userType %>)
        </div>

        <div class="list-group">
            <%# Lien visible uniquement par TOUS les utilisateurs connectés %>
            <a href="meetings" class = "list-group-item list-group-item-action">
                Consulter mes réunions
            </a>

            <%# Lien visible SEULEMENT par les organisateurs %>
            <% if (user.userType === 'organizer') { %>
                <a href="/meetings/new" class="list-group-item list-group-item-action">
                     Planifier une nouvelle réunion
                </a>
            <% } %>

            <%# Lien de déconnexion %>

            <a href="/auth/logout" class="list-group-item list-group-item-action text-danger">
                Se déconnecter
            </a>

        </div>

        <% } else { %>

            <%# --- Section pour utilisateur DÉCONNECTÉ --- %>
            <p>Veuillez vous connecter ou créer un compte pour continuer.</p>
            <a href="/auth/login" class="btn btn-primary">Se connecter</a>
            <a href="/auth/register" class="btn btn-secondary">S'inscrire</a>
        <% } %>
     

    </body>
</html>