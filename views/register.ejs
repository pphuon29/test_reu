<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    </head>
<body>
    <h1>Inscription</h1>

    <form action="/auth/register" method="POST">
        
        <div>
            <label for="reg-email">E-mail</label>
            <input type="email" name="email" id="reg-email" required
            value="<%= (typeof formData !== 'undefined' && formData.email) ? formData.email : '' %>">
            <%# Si formData.email existe, le champ est préremplie avec cette valeur, sinon nul%>
        </div>

        <div>
            <label for="reg-name">Nom</label>
            <input type="text" name="name" id="reg-name" required
            value="<%= (typeof formData !== 'undefined' && formData.name) ? formData.name : '' %>">
        </div>

        <div>
            <label for="reg-password">Mot de passe</label>
            <input type="password" name="password" id="reg-password" required>
        </div>

        <div>
            <label for="reg-confirmPassword">Mot de passe (confirmation)</label>
            <input type="password" name="confirmPassword" id="reg-confirmPassword" required>
        </div>
            
        <div>
            <label>Type de compte :</label>

            <% const choiceType = (typeof formData !== 'undefined' && formData.userType) ? formData.userType : '' %>
            <input type="radio" id="type-organizer" name="userType" value="organizer" required <%= choiceType === 'organizer' ? 'checked' : '' %>>
            <label for="type-organizer">Organisateur</label>
        

            <input type="radio" id="type-participant" name="userType" value="participant" required <%= choiceType === 'participant' ? 'checked' : '' %>>
            <label for="type-participant">Participant</label>
        </div>

        <div>
            <button type="submit">S'inscrire</button>
        </div>
        

    </form>


    </body>
</html>