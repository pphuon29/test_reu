<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1><%= meeting.title %></h1>
        <% if (meeting.description) { %>
            <p class="lead"><%= meeting.description %></p>
        <% } %>
        <hr>
        <h2>Créneaux proposés :</h2>

        <% if (slots && slots.length > 0) { %>
            <ul class="list-group">
                <% slots.forEach(slot => { %>
                    <li class="list-group-item">
                        <% const startTime = new Date(slot.start_time).toLocaleString('fr-FR', { dateStyle: 'full', timeStyle: 'short' }); %>
                        <% const endTime = new Date(slot.end_time).toLocaleString('fr-FR', { timeStyle: 'short' }); %>
                        <strong><%= startTime %></strong> à <strong><%= endTime %></strong>
                        (ID Créneau: <%= slot.slot_id %>) <%# Juste pour info %>

                        <%# --- Zone où les participants répondront (à ajouter plus tard) --- %>

                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>Aucun créneau n'a été proposé pour cette réunion.</p>
        <% } %>

        <hr>
        <a href="/" class="btn btn-secondary">Retour à l'accueil</a>
         <%# TODO: Ajouter bouton Inviter/Voir réponses pour l'organisateur %>
         <%# TODO: Ajouter formulaire de réponse pour les participants %>

    </div>
    </body>
</html>